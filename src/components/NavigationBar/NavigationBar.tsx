import React from 'react';
import styled from 'styled-components';

const Nav = styled.div`
  width: 100%;
  height: 60px;
  background-color: #212121;
  margin-bottom: 20px;
`;

export const NavigationBar = () => <Nav />;
